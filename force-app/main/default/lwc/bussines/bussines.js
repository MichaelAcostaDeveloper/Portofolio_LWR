import { LightningElement } from 'lwc';
import IMAGES from '@salesforce/resourceUrl/ImagenesSites';

export default class Bussines extends LightningElement {
    imageURL = IMAGES + '/edificio.jpg';
}